def full_name(first, last):     # inputs are first and last (arguments)
      return f'{first} {last}'  # return statement --going to return value


kristine = full_name('Kristine', 'Hudgens')   # set it inside and store it inside the kristine vairable

def greeting(name):             # name is the argument
  print(f'Hi {name}!')          


greeting(kristine)         #called greeting function and passed in the kristine variable

#steps we followed

# created fullname function
# returns the value-formatted string
# called the function
# stored full name inside kristine variable
